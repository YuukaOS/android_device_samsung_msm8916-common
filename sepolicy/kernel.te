#============= kernel ==============
allow kernel self:capability dac_override;
allow kernel sysfs:file { open read };
allow kernel untrusted_app:fd use;
allow kernel exfat:dir search;
allow kernel vfat:dir search;
allow kernel appdomain_tmpfs:file { write };
